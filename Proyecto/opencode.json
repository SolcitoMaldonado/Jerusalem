{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "netlify": {
      "type": "local",
      "command": ["npx", "-y", "@netlify/mcp"],
      "enabled": true,
      "environment": {
        "NETLIFY_AUTH_TOKEN": "{file:.netlify-token}"
      }
    }
  }
}